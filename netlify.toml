[build]
  command   = "npm run build"
  publish   = "dist"

# SPA routing — all paths serve index.html so React Router handles navigation
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
